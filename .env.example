# Cosmos DB Configuration
# For Azure Cosmos DB: https://your-account.documents.azure.com:443/
# For local Cosmos DB Emulator: https://localhost:8081
COSMOS_ENDPOINT=https://your-account.documents.azure.com:443/

# Authentication Mode: KEY or DEFAULT_AZURE_CREDENTIAL
# - KEY: Use key-based authentication (default, requires COSMOS_KEY)
# - DEFAULT_AZURE_CREDENTIAL: Use Azure identity (Managed Identity, Azure CLI, etc.)
COSMOS_AUTH_MODE=KEY

# Required when COSMOS_AUTH_MODE=KEY
COSMOS_KEY=your-primary-key-here

COSMOS_DEFAULT_DB=ureca_evo

# Optional: Logging level (DEBUG, INFO, WARN, ERROR)
LOG_LEVEL=INFO
